<div class="row">
    <div class="col-xs-12">
        <!-- /.box -->

        <div class="box">
            <div class="box-header">
                <h3 class="box-title">All Statements</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body" style="overflow-x:auto;">
                <table id="example1" class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>VendorName</th>
                            <th>ProductName</th>
                            <th>RedeemedDate</th>
                            <th>TotalDue</th>
                            <th>Edit</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each statements}}
                        <tr>
                            <td>{{vendor_id.vendor_name}}</td>
                            <td>{{product_id.name}}</td>
                            <td>{{generateTime redeemedDate 'MMMM Do YYYY'}}</td>
                            <!-- <td>{{generateTime redeemedDate 'h:mm:ss a'}}</td> -->
                            <td>{{due}}</td>
                            <td>
                                <a href="/admin/statements/edit/{{id}}" class="btn btn-info">Edit</a>
                            </td>
                            <td>
                                <form action="/admin/statements/{{id}}?_method=DELETE" method="POST">
                                    <button type="submit" class="btn btn-danger">DELETE</button>
                                </form>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    </div>
    <!-- /.col -->
</div>
<!-- /.row -->
{{!-- <script type="text/javascript" src="/js/jquery/jquery.min.js"></script> --}}
<!--jquery is must for data table for search and pages-->
<script src="/wlkasset/bower_components/jquery/dist/jquery.min.js"></script>
<!-- page script -->
<script>
    $(function () {
        $('#example1').DataTable()
        $('#example2').DataTable({
            'paging': true,
            'lengthChange': false,
            'searching': false,
            'ordering': true,
            'info': true,
            'autoWidth': false
        })
    });
</script>